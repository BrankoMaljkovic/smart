https://git-scm.com/download/win - git za instalaciju
https://github.com/ - git hub
https://about.gitlab.com/ - github
https://bitbucket.org/ - isto se koristi za to
kreirao github na biomove2014

https://www.gitkraken.com/ - tools
https://git-fork.com/ - tools


repositories - fascikla, jedan projekat jedan repositorijum

<!-- Koraci u GIT funkcije -->

$ git clone - kopiramo url kreiranog prository u GitHub-u
clear - brise se sve sa terminal ekrana
konfigurisemo username i email
$ git config --global user.name "BrankoMaljkovic"
$ git config --global user.email "brankomaljkovic@yahoo.com"
$ git config --list (da proverimo da li je ulistano)
cd folder (ulazak u novi folder)

$ git status (govori sta imamo od fajlova i u kom su statusu crveno nije sacuvano, zeleno je odradjen add)

$ git add . (zelimo da kazemo sta da sacuvamo . je sve ili navedemo fajl ili folder ime)

$ git commit -m "" (lokalno cuvamo na racunaru, -m je poruka "text poruke")

$ git push (guramo sa lokalnog na web)

$ git pull (proveravamo da li je doslo do promene verzije online, odnosno da li je neko vrsio promene u medjuvremenu)

$ git log (istorija svih logova)

<!-- Kreiranje grana -->

$ git branch version01 (kreiramo granu, version01 je ime grane koju kreiramo)
$ git branch (proveravamo grane)
$ git checkout version01 (za prebacivanje na drugu granu version01 je ime grane na koju se prebacujemo)

$ git push origin version01 (upload branch u GIT)
$ git pull origin version01 (kada samo u branchu moramo da kucamo origin da oslovimo glavnu liniju konekcije i naziv brancha sa kog pozivamo)

<!-- Kreiranje grana uvek idemo na granu na koju spajamo pa spajamo druge grane -->

$ git merge version01 (stavljam branch koji zelim da kopiram)
$ git push (na kraju svake promene moramo da uradimo push i gurnemo to online)